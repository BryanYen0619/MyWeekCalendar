<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <!-- stroke 描邊 -->
    <stroke
        android:width="1dp"
        android:color="@color/colorTropicalOrange50"/>

    <!-- corners 圓角 -->
    <corners
        android:radius="@dimen/img_radius"/>

    <!-- gradient 漸層 -->
    <gradient
        android:angle="0"
        android:endColor="@color/colorTropicalOrange"
        android:startColor="@color/colorTropicalOrange"
        android:type="linear"/>
</shape>